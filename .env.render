# ==============================================
# RENDER PRODUCTION ENVIRONMENT VARIABLES
# ==============================================
# Copy ALL lines below and paste into Render Dashboard
# Go to: Your Web Service → Environment → Add Environment Variable
# Paste each line (Render will parse them automatically)
# ==============================================

# Django Secret Key (REQUIRED - Keep this secret!)
SECRET_KEY=django-insecure-$6t9fx@7m_h=y0p+0@2h@k4^l4w^4x3$_qn8z@5r3c_e6m=v4g

# Debug Mode (MUST be False in production)
DEBUG=False

# Database URL (Your PostgreSQL from Render)
# NOTE: Use the INTERNAL URL on Render, not this external one!
# This external URL is only for testing from your local machine
DATABASE_URL=postgresql://digitrackdb_jt2h_user:1qdgW2cBOzwle4zCJRCK0IW0pCK4gDE0@dpg-d3v0g375r7bs73fs0vgg-a/digitrackdb_jt2h

# Superuser Credentials (for automatic admin creation)
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@digitrackdb.com
DJANGO_SUPERUSER_PASSWORD=Admin@DigiTrack2025!

# ==============================================
# IMPORTANT NOTES:
# ==============================================
# 1. On Render, replace DATABASE_URL with the INTERNAL URL
#    (Find it in: PostgreSQL Database → Internal Database URL)
#    It should start with: postgresql://...dpg-...-a.oregon-postgres.render.com/...
#
# 2. Change DJANGO_SUPERUSER_PASSWORD to your own strong password!
#
# 3. Generate a NEW SECRET_KEY by running:
#    python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# ==============================================
